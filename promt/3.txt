Привет! Проект на FastAPI + Celery + PostgreSQL успешно прошёл аудит безопасности (Bandit: 0 проблем) и чистоты кода (Skylos: Grade A+ 100). Нужно реализовать финальные улучшения для достижения production-ready уровня. Пожалуйста, сделай следующие доработки по очереди:

    Валидация загружаемых файлов по Magic Bytes:

        Добавить проверку реальной байтовой сигнатуры (magic numbers) для загружаемых файлов на эндпоинте FastAPI, исключив подмену расширений и типов.

    Структурированное логирование (structlog):

        Внедрить structlog для вывода логов в формате JSON. Реализовать проброс сквозного request_id из HTTP-запроса FastAPI в фоновые задачи Celery.

    Настройка CI/CD (GitHub Actions):

        Создать файл конфигурации .github/workflows/ci.yml для автоматического запуска линтеров (bandit, skylos) и тестового контура (pytest) при каждом пуше и пулл-реквесте.

    Обогащение OpenAPI документации:

        Добавить явные схемы response_model, описания через Field и примеры полезной нагрузки (openapi_examples) для ключевых эндпоинтов.

Напиши чистый код, соответствующий текущей архитектуре проекта, без лишних абстракций.